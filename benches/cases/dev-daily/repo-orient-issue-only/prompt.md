Use the `repo-orient` skill.

We have a bug where webhook deliveries sometimes get marked successful even
though the provider returned a 429. I do not want a patch yet. Orient to the repo
and tell me where you would inspect first, what tests or logs would matter, and
what you cannot know until you see the relevant files.
