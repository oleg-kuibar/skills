Use the `repo-orient` skill.

Developer prompt:

```text
Before we change refund retry behavior, orient on the payment flow and tell me where to start.
```

Use the context pack in `context-pack.md` as the repository and session context
that the harness supplied.
